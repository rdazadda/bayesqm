% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{factor_characteristics}
\alias{factor_characteristics}
\title{Factor characteristics}
\usage{
factor_characteristics(fit, prob = 0.9, q = 0.05, floor = 0.5)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{prob}{Credible-interval probability for the defining-sort counts
(default 0.90).}

\item{q, floor}{The flag rule fixing the flagged set, as in
\code{\link[=compute_flags]{compute_flags()}}.}
}
\value{
A data frame with one row per factor: \code{factor}, \code{flagged}
(selected flags), \code{defining_modal}, \code{defining_mean},
\code{defining_lower}, \code{defining_upper}, \code{score_spread}, and
\code{reliability}. Attribute \code{score_correlations} holds the posterior
mean K x K correlation matrix of the statement scores.
}
\description{
The per-factor block of the accompanying paper: modal and mean number
of defining sorts per posterior draw with a credible interval, the
selected flag count, the mean posterior spread of the statement
scores, and the mean replicate reliability
\code{R_i = s_i^2 / (1 + s_i^2)} of the flagged participants. The
statement-score correlations between factors ride along as an
attribute.
}
\examples{
factor_characteristics(demo_fit())

}
