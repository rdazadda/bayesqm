% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/membership.R
\name{critical_delta}
\alias{critical_delta}
\title{Bayesian reliability-adjusted critical difference (default \code{delta})}
\usage{
critical_delta(Lambda_draws, level = 0.05, r0 = 0.8)
}
\arguments{
\item{Lambda_draws}{Array of shape \verb{[T, N, K]} of aligned loading
draws.}

\item{level}{Two-sided level for the critical value, \code{z = qnorm(1 - level/2)}. \code{0.05} (default) gives \code{z = 1.96}; \code{0.01} gives
\code{z = 2.58}. Report sensitivity over \code{level}.}

\item{r0}{Conventional single-sort reliability (default \code{0.80};
Brown 1980).}
}
\value{
A single numeric value, the critical-difference \code{delta}.
}
\description{
The default separation for the distinguishing and consensus
probabilities: a Bayesian reliability-adjusted critical difference
(Brown 1980; Zabala & Pascual 2016). With \code{p_f} the number of
participants whose posterior dominant factor is \code{f},
\verb{r_f = p_f r0 / (1 + (p_f - 1) r0)} and \code{SE_f = sqrt(1 - r_f)},
\verb{delta = z * mean_\{k < l\} sqrt(SE_k^2 + SE_l^2)}. The reliability is
the stable population reliability of the design, not the posterior
estimation spread.
}
\examples{
critical_delta(array(rnorm(200 * 8 * 3), c(200, 8, 3)))

}
