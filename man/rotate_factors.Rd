% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotation.R
\name{rotate_factors}
\alias{rotate_factors}
\title{Rotate every aligned draw toward a target}
\usage{
rotate_factors(fit, target, oblique = FALSE)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{target}{A \verb{J x K} numeric matrix of target scores (for example, a
hand-edited copy of the posterior-mean scores).}

\item{oblique}{Allow an oblique (non-orthogonal) rotation
(default \code{FALSE}).}
}
\value{
The fit with rotated draws; \code{fit$align$rotated} records the
call.
}
\description{
Judgmental rotation: each aligned draw's statement scores are rotated
toward an analyst-specified target by Procrustes, and the loadings
receive the same rotation (its inverse transpose when \code{oblique = TRUE}),
so the latent utilities every draw implies are unchanged. Summaries
computed from the returned fit are summaries of the rotated solution,
with full posterior uncertainty.
}
