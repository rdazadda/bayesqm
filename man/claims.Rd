% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claims.R
\name{claims}
\alias{claims}
\title{Selected claims at a common false-discovery level}
\usage{
claims(fit, q = 0.05, flag_floor = 0.5, cons_floor = 0.95)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{q}{Posterior expected false-discovery bound (default 0.05).}

\item{flag_floor, cons_floor}{Selection floors for flags (0.5) and
consensus statements (0.95).}
}
\value{
A \code{bayesqm_claims} object: selected-only tables \code{flags},
\code{distinguishing}, \code{consensus}, and \code{stars}, each with its
\code{expected_false} count, plus the level \code{q}.
}
\description{
Selects every claim — participant flags, distinguishing listings,
consensus statements, and pairwise stars — by the one posterior
false-discovery rule at level \code{q}, and reports the expected number of
false claims per family alongside. The same code path produces the
\code{selected} columns of \code{\link[=compute_flags]{compute_flags()}} and \code{\link[=compute_qdc]{compute_qdc()}}, so the
tables and this object always agree.
}
\examples{
claims(demo_fit())

}
