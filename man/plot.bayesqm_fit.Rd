% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.bayesqm_fit}
\alias{plot.bayesqm_fit}
\title{Factor-score dotchart for a bayesqm_fit}
\usage{
\method{plot}{bayesqm_fit}(x, sort_by = 1L, prob = NULL, cex.lab = 0.7, ...)
}
\arguments{
\item{x}{A \code{bayesqm_fit}.}

\item{sort_by}{Integer factor index whose posterior mean is used to
sort rows (default 1).}

\item{prob}{Outer-interval coverage probability; defaults to
\code{fit$brief$prob}.}

\item{cex.lab}{Axis-label text size.}

\item{...}{Additional arguments forwarded to \code{plot()}.}
}
\value{
The input, invisibly.
}
\description{
One panel per factor: horizontal dotchart of every statement's
posterior median z-score with nested 50 percent (thick) and
\code{prob}-coverage (thin) credible-interval whiskers. Statements are
sorted once -- by default, by the first factor's posterior mean --
and that ordering is reused across panels so the reader can scan
horizontally to compare factors. For a loadings-only view, see
\code{\link[=plot_loading_posterior]{plot_loading_posterior()}}; for a single statement across factors,
see \code{\link[=plot_zscore_posterior]{plot_zscore_posterior()}}.
}
