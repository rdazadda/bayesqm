% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selection.R
\name{run_bayes}
\alias{run_bayes}
\alias{select_k_peak}
\alias{select_k_sivula}
\title{Fit the model across a range of K}
\usage{
run_bayes(
  Y,
  K_max = 5,
  stan_dir = NULL,
  elpd_diff_threshold = 4,
  se_ratio_threshold = 2,
  ...
)

select_k_peak(elpds, K_candidates)

select_k_sivula(
  elpds,
  loo_list,
  K_candidates,
  elpd_diff_threshold = 4,
  se_ratio_threshold = 2
)
}
\arguments{
\item{Y}{A \code{qsort_data} object or \verb{J x N} numeric matrix.}

\item{K_max}{Largest K to try (default 5).}

\item{stan_dir}{Optional override of the Stan model directory.}

\item{elpd_diff_threshold, se_ratio_threshold}{Sivula rule thresholds
(defaults 4 and 2).}

\item{...}{Passed to \code{\link[=fit_bayesian]{fit_bayesian()}}.}

\item{elpds, loo_list, K_candidates}{Internal inputs for
\code{select_k_peak()} and \code{select_k_sivula()}.}
}
\value{
\code{run_bayes()} returns a \code{bayesqm_run} object carrying the
list of fits, the ELPD comparison table, and the peak / Sivula /
case verdict. \code{select_k_peak()} and \code{select_k_sivula()} return an
integer K.
}
\description{
Fits \code{\link[=fit_bayesian]{fit_bayesian()}} for K = 1..K_max and reports the ELPD peak
(automated adoption), the Sivula (2025) parsimony diagnostic, and a
case label (\code{agree}, \code{gap}, \code{reversed}) summarising their
relationship. When the two agree the data supports that K; when they
disagree the \code{gap} is itself a reportable finding.
}
