% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{compute_factor_array}
\alias{compute_factor_array}
\title{Quota-respecting factor arrays}
\usage{
compute_factor_array(fit, negative = FALSE)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{negative}{Also return the negative-pole arrays (default \code{FALSE});
on a symmetric grid the mirror is exact.}
}
\value{
A data frame with one row per statement: \code{statement}, then
\verb{f\{k\}_grid} per factor (and \verb{f\{k\}_grid_neg} when \code{negative = TRUE}).
\code{attr(, "footrule_disagreement")} gives the share of cells where the
footrule assignment differs (\code{NA} without \pkg{clue}).
}
\description{
The reported array for each factor: statements sorted onto the design
grid by their posterior column probabilities (mean grid column,
tie-broken by posterior-mean score), which is quota-exact by
construction. When the \pkg{clue} package is installed, a footrule
assignment cross-check is computed and its disagreement rate attached.
}
\examples{
compute_factor_array(demo_fit())

}
