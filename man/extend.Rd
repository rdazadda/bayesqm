% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bayesian.R
\name{extend}
\alias{extend}
\title{Continue sampling a fitted chain}
\usage{
extend(fit, iterations = NULL, pivot = NULL, quiet = FALSE)
}
\arguments{
\item{fit}{A \code{bayesqm_fit} created with \code{keep_raw = TRUE}.}

\item{iterations}{Additional iterations; \code{NULL} (the default) doubles
the chain. Must be a multiple of the fit's \code{thin}.}

\item{pivot, quiet}{As in \code{\link[=fit_bayesian]{fit_bayesian()}}.}
}
\value{
The extended \code{bayesqm_fit}.
}
\description{
Warm-extends the fitted chain by further iterations and re-runs the
convergence gate and the alignment on the grown draw set. The extension
restores the sampler's saved random-number state, so the result is
draw-for-draw identical to having run one longer chain, whatever
happened in the session in between.
}
