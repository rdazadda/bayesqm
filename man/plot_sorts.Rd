% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sorts.R
\name{plot_sorts}
\alias{plot_sorts}
\title{Preview every participant's sort}
\usage{
plot_sorts(x, participants = NULL, per_page = 12, labels = NULL, cex = NULL)
}
\arguments{
\item{x}{A \code{qsort_data} object or a \verb{J x N} matrix of sorts.}

\item{participants}{Which participants to draw (names or indices);
\code{NULL} (default) draws everyone.}

\item{per_page}{How many pyramids per page (default up to 12,
arranged automatically); further participants continue on the next
page.}

\item{labels}{Optional statement labels for the tiles (defaults to the
statement ids).}

\item{cex}{Tile label size; \code{NULL} (default) adapts to the tallest
column.}
}
\value{
The input, invisibly.
}
\description{
Draws each participant's completed sort as their own pyramid: one tile
per statement, stacked in its grid column, colored along the
disagree-agree axis. This is the look-at-the-decisions-first view:
run it straight after import, before any analysis, to see how every
participant grouped the statements. \code{plot(qdata)} does the same.
}
\examples{
distr <- c(1, 2, 3, 2, 1)
set.seed(1)
Y <- replicate(4, {
  r <- integer(9)
  r[order(rnorm(9))] <- rep(seq_along(distr), distr)
  r - 3                                  # printed labels -2..+2
})
plot_sorts(qsort_data(Y, distribution = distr, validate = FALSE))

}
