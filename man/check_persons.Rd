% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{check_persons}
\alias{check_persons}
\title{The person check against mixed-replication bands}
\usage{
check_persons(fit, draws = 60, mixes = 150)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{draws}{Draw grid for the arrays (default 60).}

\item{mixes}{Mixed replicates for the bands (default 150).}
}
\value{
A \code{bayesqm_persons} data frame: \code{participant}, \code{m}, \code{w},
\code{partner}, \code{verdict}, with the band limits attached as attributes.
}
\description{
For each participant, the model-agreement statistic \code{m} (mean over
draws of the best absolute Spearman agreement with any factor array)
and the person-agreement statistic \code{w} (best absolute agreement with
any other sort), each located against bands from mixed replicates
(fresh persons drawn from the fitted model). Verdicts: \code{fits},
\code{no_shared} (below the model band, inside the person band),
\code{unspanned} (below the model band but above the person band, a shared
viewpoint the fitted factors do not span), and \code{atypical}. Unspanned
persons name their nearest partner.
}
\examples{
check_persons(demo_fit(), draws = 20, mixes = 40)

}
