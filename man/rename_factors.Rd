% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_factors.R
\name{rename_factors}
\alias{rename_factors}
\title{Rename factors consistently across a bayesqm_fit}
\usage{
rename_factors(fit, new_names)
}
\arguments{
\item{fit}{A \code{bayesqm_fit} object.}

\item{new_names}{Character vector of length \code{K} with the new factor
labels.}
}
\value{
The input fit with every factor label replaced.
}
\description{
Replaces the default \verb{f1..fK} factor labels everywhere they appear on
the fit: posterior-mean and credible-interval loading matrices, the
factor-score matrices, the factor-characteristics table, the
correlation matrix, the flagged matrix, the distinguishing /
consensus table column names, and the factor dimension of the raw
\code{Lambda_draws} / \code{F_draws} arrays.
}
\examples{
\dontrun{
fit <- fit_bayesian(Y, K = 3)
fit <- rename_factors(fit, c("tradition", "innovation", "caution"))
}

}
