% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{check_fit}
\alias{check_fit}
\title{Posterior-predictive checks of the fitted model}
\usage{
check_fit(fit, draws = 100)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{draws}{Posterior draws used for replication (default 100).}
}
\value{
A \code{bayesqm_checks} list: \code{agreement} (\code{p}), \code{extra_factor}
(\code{percentile}), and \code{paired} (\code{p}, per-statement \code{p_j}), with a print
method.
}
\description{
Three checks, each comparing the observed sorts with replicates drawn
from the fitted model:
the agreement check (T1a) compares the observed person-agreement matrix
with replicated ones against a double-replicate reference; the
extra-factor check (T1b) locates the observed \code{(K+1)}-th eigenvalue in
its replicated distribution; and the paired-comparison check (T2)
compares standardized statement-pair margins, ties counted one half.
These are diagnostics to read, not tests to pass.
}
\examples{
check_fit(demo_fit(), draws = 20)

}
