% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/membership.R
\name{bayesqm-membership}
\alias{bayesqm-membership}
\alias{compute_threshold_prob}
\alias{compute_dominant_prob}
\alias{compute_dominant_sign}
\alias{compute_divergence}
\alias{classify_membership}
\title{Probabilistic factor-membership and divergence summaries}
\usage{
compute_threshold_prob(Lambda_draws, threshold)

compute_dominant_prob(Lambda_draws)

compute_dominant_sign(Lambda_draws)

compute_divergence(F_draws, delta = NULL, delta_grid = NULL)

classify_membership(Lambda_draws, strong = 0.8, moderate = 0.6)
}
\arguments{
\item{Lambda_draws}{Array of shape \verb{[T, N, K]} of aligned loading
draws.}

\item{threshold}{Numeric threshold; a natural default is
\code{1.96 / sqrt(J)} for the Brown rule.}

\item{F_draws}{Array of shape \verb{[T, J, K]} of aligned factor-score
draws.}

\item{delta}{Substantive separation for the distinguishing and
consensus probabilities. The fit pipeline supplies the default,
the reliability-adjusted critical difference of \code{\link[=critical_delta]{critical_delta()}};
\code{\link[=suggest_delta]{suggest_delta()}} is an alternative. If \code{NULL} the \code{D_j} posterior
(median, 95\% CrI, \code{g_jk}) is still returned but the
distinguishing/consensus probabilities are \code{NA}.}

\item{delta_grid}{Optional numeric vector of \code{delta} values for a
sensitivity sweep.}

\item{strong, moderate}{Tier cutoffs on \verb{max P(dominant)} (defaults
0.80 and 0.60).}
}
\value{
\code{compute_threshold_prob()} and \code{compute_dominant_prob()}
return \verb{N x K} matrices. \code{compute_dominant_sign()} returns a
length-\code{N} named vector. \code{compute_divergence()} returns a list
(see Details). \code{classify_membership()} returns a data frame.
}
\description{
Posterior summaries of factor membership and statement
interpretation, each computed entirely from posterior draws:
\itemize{
\item \code{compute_threshold_prob()} returns the \verb{N x K} posterior
probability that \verb{|lambda_ik| > threshold}, i.e. the Bayesian
version of the Brown (1980) flagging rule.
\item \code{compute_dominant_prob()} returns the \verb{N x K} posterior
probability that factor \code{k} is the dominant factor for participant
\code{i}.
\item \code{compute_dominant_sign()} returns the length-\code{N} posterior
probability that the dominant loading is positive; participants
with probability below 0.5 are negative exemplars.
\item \code{compute_divergence()} returns the posterior of the viewpoint
divergence \code{D_j} for every statement, together with the
distinguishing and consensus probabilities it implies.
\item \code{classify_membership()} turns dominant probabilities into a
per-participant descriptive tier (Strong / Moderate / Weak).
}
}
\details{
For statement \code{j} with standardized viewpoint scores
\verb{f_\{j1\}, ..., f_\{jK\}}, the divergence estimand is the mean absolute
pairwise difference
\verb{D_j = 2 / (K (K - 1)) * sum_\{k < l\} |f_jk - f_jl|}.
The mean is used rather than the maximum, which is an order
statistic over the \code{K (K - 1) / 2} contrasts and is inflated when
the posterior is diffuse. \code{compute_divergence()} reports the
posterior median and central 95\% credible interval of \code{D_j},
\code{pi_D = P(D_j > delta | Y)} (distinguishing) and
\code{pi_C = P(D_j < delta | Y) = 1 - pi_D} (consensus), and the
per-viewpoint departure \verb{g_jk = f_jk - mean_\{l != k\} f_jl} with its
dominant viewpoint, sign, and \verb{P(|g_jk| > delta | Y)}. The
probabilities are the reported quantities; no fixed probability
cutoff defines a distinguishing or consensus statement.
}
