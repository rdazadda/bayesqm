% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/membership.R
\name{bayesqm-membership}
\alias{bayesqm-membership}
\alias{compute_threshold_prob}
\alias{compute_dominant_prob}
\alias{compute_distinguishing_prob}
\alias{compute_consensus_prob}
\alias{classify_membership}
\title{Probabilistic factor-membership summaries}
\usage{
compute_threshold_prob(Lambda_draws, threshold)

compute_dominant_prob(Lambda_draws)

compute_distinguishing_prob(F_draws, delta = 1)

compute_consensus_prob(F_draws, delta = 1)

classify_membership(Lambda_draws, strong = 0.8, moderate = 0.6)
}
\arguments{
\item{Lambda_draws}{Array of shape \verb{[T, N, K]} of aligned loading
draws.}

\item{threshold}{Numeric threshold; a natural default is
\code{1.96 / sqrt(J)} for the Brown rule.}

\item{F_draws}{Array of shape \verb{[T, J, K]} of aligned factor-score
draws.}

\item{delta}{Minimum separation on the standardized factor-score
scale (default \code{1.0}).}

\item{strong, moderate}{Tier cutoffs on \code{max P(dominant)} (defaults
0.80 and 0.60 following the paper).}
}
\value{
\code{compute_threshold_prob()}, \code{compute_dominant_prob()} return
\verb{N x K} matrices. \code{compute_distinguishing_prob()} returns a
\verb{J x choose(K, 2)} matrix. \code{compute_consensus_prob()} returns a
length-\code{J} named vector. \code{classify_membership()} returns a data
frame.
}
\description{
The paper's Section 6 summaries, each computed entirely from posterior
draws:
\itemize{
\item \code{compute_threshold_prob()} returns the \verb{N x K} posterior
probability that \code{|lambda_ik| > threshold}, i.e. the Bayesian
version of the Brown (1980) flagging rule.
\item \code{compute_dominant_prob()} returns the \verb{N x K} posterior
probability that factor \code{k} is the dominant factor for participant
\code{i}.
\item \code{compute_distinguishing_prob()} returns \code{P(|f_jk - f_jl| > delta)}
for every pair of factors.
\item \code{compute_consensus_prob()} returns the joint probability that
\emph{every} factor pair's separation stays below \code{delta}.
\item \code{classify_membership()} turns dominant probabilities into a
per-participant tier verdict (Strong / Moderate / Weak).
}
}
