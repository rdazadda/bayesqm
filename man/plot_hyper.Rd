% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_hyper}
\alias{plot_hyper}
\title{Hyperparameter posterior densities}
\usage{
plot_hyper(fit, pars = c("nu", "sigma", "tau"), log = NULL, ...)
}
\arguments{
\item{fit}{A \code{bayesqm_fit}.}

\item{pars}{Character vector of hyperparameter names to show.}

\item{log}{\code{NULL} (default) auto-detects per parameter; set to
\code{"x"} to force log, or \code{""} to force linear.}

\item{...}{Additional arguments forwarded to \code{plot()}.}
}
\value{
The input, invisibly.
}
\description{
One panel per scalar hyperparameter (default \code{nu}, \code{sigma}, \code{tau})
showing a two-tone kernel density: the full posterior in the light
shade, the central credible interval re-shaded in the mid shade,
and the posterior median marked by a short tick at the baseline.
Coverage defaults to \code{fit$brief$prob}. When a parameter's support
spans more than one order of magnitude (\code{max / min > 20}, all
positive), the x-axis is automatically rendered on a log scale so
the density shape is not crushed against the lower bound.
}
