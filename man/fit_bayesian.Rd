% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{fit_bayesian}
\alias{fit_bayesian}
\title{Fit a Bayesian Q-methodology factor model}
\usage{
fit_bayesian(
  Y,
  K,
  stan_dir = NULL,
  robust = TRUE,
  nu = "estimate",
  chains = 4,
  iter = 2000,
  warmup = 1000,
  seed = NULL,
  adapt_delta = 0.9,
  max_draws = 2000,
  prior_loading_scale = 1,
  prior_sigma_scale = 1,
  prior_nu_alpha = 2,
  prior_nu_beta = 0.1,
  use_half_cauchy = FALSE,
  prob = 0.95
)
}
\arguments{
\item{Y}{Either a \code{qsort_data} object or a \verb{J x N} numeric matrix with
statements as rows and participants as columns.}

\item{K}{Integer number of factors to extract.}

\item{stan_dir}{Directory containing \code{stan/factor_model.stan}. \code{NULL}
(the default) uses the copy shipped in \code{inst/stan/}.}

\item{robust}{Logical; \code{TRUE} uses a Student-t likelihood, \code{FALSE}
uses Normal.}

\item{nu}{Either \code{"estimate"} (default) to sample the Student-t
degrees of freedom, or a numeric value (e.g. \code{5}, \code{Inf}) to fix it.}

\item{chains, iter, warmup}{NUTS sampler settings.}

\item{seed}{Optional integer seed for reproducibility.}

\item{adapt_delta}{NUTS adapt_delta target (default 0.90).}

\item{max_draws}{Thin post-warmup draws to at most this many before
MatchAlign (default 2000).}

\item{prior_loading_scale, prior_sigma_scale, prior_nu_alpha, prior_nu_beta, use_half_cauchy}{Prior hyperparameters (see the Stan model for parameterization).}

\item{prob}{Credible-interval probability stored on the fit (default 0.95).}
}
\value{
A \code{bayesqm_fit} object. See \link{bayesqm-fit-methods} for \code{print()}
and \code{summary()}, and \code{\link[=coef.bayesqm_fit]{coef.bayesqm_fit()}} for the standard R
accessors.
}
\description{
Fits the low-rank Bayesian factor model to Q-sort data. Samples the
posterior with Stan (via \pkg{cmdstanr} or \pkg{rstan}), resolves
rotational ambiguity with MatchAlign, and returns a classed
\code{bayesqm_fit} object carrying posterior-mean loadings and factor
scores, credible intervals, raw draws, LOO, PPC, and diagnostics.
}
\examples{
\dontrun{
qdata <- read_qsort("mystudy.csv")
fit <- fit_bayesian(qdata, K = 3)
fit
summary(fit)
}

}
\references{
Dacosta Azadda, R. (2026). Bayesian Q Methodology: Probabilistic Factor
Analysis for the Study of Subjectivity.

Poworoznek et al. (2025). Efficiently Resolving Rotational Ambiguity in
Bayesian Matrix Sampling with Matching. \emph{Bayesian Analysis}.
}
