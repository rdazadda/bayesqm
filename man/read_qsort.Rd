% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_qsort}
\alias{read_qsort}
\alias{read_qsort_csv}
\alias{read_qsort_excel}
\alias{read_pqmethod}
\alias{read_kenq}
\alias{read_kenq_excel}
\alias{read_kade_zip}
\alias{read_easyhtml_firebase}
\alias{read_statements}
\title{Read Q-sort data from file}
\usage{
read_qsort(file, format = "auto", ...)

read_qsort_csv(
  file,
  orientation = c("auto", "statements_rows", "participants_rows"),
  id_col = c("auto", "first", "none"),
  statements = NULL,
  distribution = NULL,
  ...
)

read_qsort_excel(
  file,
  sheet = 1,
  orientation = c("auto", "statements_rows", "participants_rows"),
  id_col = c("auto", "first", "none"),
  statements = NULL,
  distribution = NULL,
  ...
)

read_pqmethod(file, statements_file = NULL)

read_kenq(file, format = c("auto", "json", "csv"))

read_kenq_excel(file)

read_kade_zip(file)

read_easyhtml_firebase(file)

read_statements(file, column = 1, id_column = NULL)
}
\arguments{
\item{file}{Path to the data file.}

\item{format}{For \code{read_qsort()}, \code{"auto"} (default) or one of
\code{"csv"}, \code{"excel"}, \code{"pqmethod"}, \code{"kenq"}, \code{"kenq_excel"},
\code{"kade"}, \code{"easyhtml_firebase"}. For \code{read_kenq()}, one of
\code{"auto"}, \code{"json"}, \code{"csv"}.}

\item{...}{Passed to the underlying reader.}

\item{orientation}{For generic CSV/Excel: \code{"auto"},
\code{"statements_rows"}, or \code{"participants_rows"}.}

\item{id_col}{For generic CSV/Excel: \code{"auto"}, \code{"first"}, or \code{"none"}.}

\item{statements, distribution}{Optional overrides passed to
\code{\link[=qsort_data]{qsort_data()}}.}

\item{sheet}{Excel sheet name or index (default \code{1}).}

\item{statements_file}{For PQMethod, optional companion statements
file.}

\item{column, id_column}{For \code{read_statements()}: column index or name.}
}
\value{
A \code{qsort_data} object, except \code{read_statements()} which
returns a named character vector.
}
\description{
\code{read_qsort()} auto-detects the file format from extension and content
and dispatches to a specialised reader. The specialised readers are
also exported for explicit use:
\itemize{
\item \code{read_qsort_csv()}, \code{read_qsort_excel()} for generic CSV / Excel
(with HTMLQ / FlashQ / Ken-Q auto-detection baked in)
\item \code{read_pqmethod()} for PQMethod \code{.DAT} files
\item \code{read_kenq()} for Ken-Q JSON or CSV
\item \code{read_kenq_excel()} for multi-sheet Ken-Q Excel (Type 1 and Type 2,
both old and Ver2 sub-formats)
\item \code{read_kade_zip()} for KADE ZIP archives
\item \code{read_easyhtml_firebase()} for Easy-HTMLQ Firebase JSON
\item \code{read_statements()} for a standalone statement-text file
}

All readers return a \code{qsort_data} object in \verb{J x N} orientation
(statements as rows, participants as columns).
}
